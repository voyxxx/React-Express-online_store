import styled from 'styled-components'

import { flexCSS } from 'src/common/styled/common.styled'

export const BrandBarStyled = styled.section`
  ${flexCSS({ alignContent: 'start', gap: '0 4px' })}
`