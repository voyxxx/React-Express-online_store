import styled from 'styled-components'
import { flexCSS } from 'src/common/styled/common.styled'

export const TypeBarStyled = styled.aside`
  ${flexCSS({ direction: 'column', align: 'start', gap: '4px 0' })}
  width: 25%;
`